# ---------------------------------------------------------------------------
# apps/frontend — copy to .env.local. NEVER commit the filled-in file.
# ---------------------------------------------------------------------------

# Base URL of the backend, including the /api/v1 prefix.
NEXT_PUBLIC_API_URL=http://localhost:3001/api/v1

# Shown in the UI footer / about box. Optional.
NEXT_PUBLIC_APP_NAME=My Finance App
